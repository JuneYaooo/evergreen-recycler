---
name: evergreen-recycler
description: 把博主自己导出的历史帖子 CSV 做平台内赢家检测（互动量对比账号中位数倍数+分位数，样本不足自动降级），生成遵守每平台每周上限、同帖再发间隔、排期日间隔的「变体重发」排期表，并为每条排期写文案变体方向与再发理由；只做本地统计与排期，不自动发布、绝不建议原样重发（小红书 180 天内容指纹、抖音判重复），并提醒用户重发不等于删除原帖。
---

# 老帖翻红排期

当用户想"翻红老帖""把以前的爆款再发一次""给老内容排再发计划"时使用本 Skill。
交付物是一份每条都能说出"为什么是它、怎么变着发"的再发排期表，不是数据分析报告。

## 工作边界

- **红线：只建议「变体重发」，绝不建议原样重发。**小红书会比对同账号 180 天内笔记的
  内容指纹（图片/文字/标题相似即判重复发布），抖音对重复内容限流，微博对重复贴文降权。
  每条排期都必须给出文案变体方向（新钩子/新角度/换开头案例），并明确告知用户
  原样重贴的风险。
- **重发不等于删除原帖。**小红书/微博删帖重发同样会被判重复并限流；原帖保留，
  用变体新发。
- 只处理用户自备的历史帖子导出 CSV，全部本地计算，零网络、不需要平台授权或 API。
- 脚本的赢家判定是**平台内相对比较**（对比账号自身中位数），不是流量承诺；
  平台限流规则（如 180 天指纹）随时可能变，间隔与配额都是参数，宁可保守。
- 历史帖 CSV 含账号经营数据：提醒用户不要公开台账原件。

## 默认流程

1. **收数据**：请用户提供历史帖子导出（CSV 或可粘贴的表格），字段见
   [references/排期规则与字段.md](references/排期规则与字段.md)；常见中文列名
   （平台/标题/发布日期/播放量/点赞/评论）自动映射，缺字段可容忍。
2. **跑排期脚本**：`python3 scripts/build_schedule.py posts.csv --as-of 基准日 -o 排期表.md --csv schedule.csv --json 汇总.json`
   （评测与复现时固定 --as-of）。脚本自动完成数据体检、赢家检测、贪心排期；
   参数与退出码见 [docs/usage.md](docs/usage.md)。
3. **看脚本给的信号**：
   - 总帖 <20 条 → 报告是「先攒数据」建议，不要硬排，向用户解释样本太少时分位数不可信；
   - 某平台样本 <10 条 → 该平台赢家「仅标记不排期」，如用户坚持要排，提醒风险并建议先攒样本；
   - 全账号没有赢家（没有人跑到中位数 3 倍）→ 如实转达"暂时没有值得翻红的帖子"，
     不硬凑排期。
4. **填理由位与变体方向**（Agent 的核心工作）：对排期表每一条——
   - 解释为什么它是赢家：结合脚本依据（"互动是账号中位数的 4.9 倍、位于前 10%"）
     和内容属性（"教程型常青需求，发布半年后仍有搜索价值"）；
   - 写 1-3 个文案变体方向，从这些角度选：**新钩子**（换首句/首图诉求）、
     **新角度**（教程 → 踩坑复盘/一年后再看）、**换开头案例**（换目标人群关键词或场景重定位）、
     **数据更新**（更新过时信息后重发）。变体必须改到"内容指纹"层面（标题、首图、开头均不同）；
   - 写明再发窗口依据（"距上次再发 81 天，满足 ≥60 天间隔"）。
5. **交付与回填**：把排期表交给用户人工确认后再发布；提醒用户发布后在
   schedule.csv 的 status 列回填 done/skipped，并把再发日期记入 ledger.json，
   下次运行脚本会自动避开 60 天内的重复安排。

## 排期硬约束（脚本内置，全部参数化）

| 约束 | 默认值 | 参数 |
| --- | --- | --- |
| 每平台排期密度 | 任意 7 天 ≤3 条 | `--weekly-cap` |
| 同帖两次再发间隔 | ≥60 天 | `--recycle-gap-days` |
| 同平台排期日间隔 | ≥1 天 | `--min-gap-days` |
| 赢家阈值 | 平台内中位数 ≥3x 且 ≥P75 | `--median-x`、`--min-percentile` |
| 平台样本门槛 | <10 条降级仅标记 | `--min-sample` |
| 冷启动门槛 | 总帖 <20 条不出排期 | 脚本内置 |

支持文件：

- [references/排期规则与字段.md](references/排期规则与字段.md)：输入 CSV 字段、
  互动量口径、ledger.json 格式、排期约束的完整定义。
- [references/benchmarks.md](references/benchmarks.md)：与 CoSchedule ReQueue、
  ContentStudio、SmarterQueue 等付费工具的规则对比与定位。
- [docs/usage.md](docs/usage.md)：命令行参数、退出码与评测复现说明。
- [evals/README.md](evals/README.md)：评测证据入口。
